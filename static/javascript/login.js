document.addEventListener('DOMContentLoaded', function() {
            var alertMessage = "wrong email or password, please enter again";
            if (alertMessage) {
                alert(alertMessage);
            }
        });